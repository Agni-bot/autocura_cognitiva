apiVersion: v1
kind: Service
metadata:
  name: monitoramento
  namespace: autocura-cognitiva
  labels:
    app: monitoramento
    component: autocura-cognitiva
spec:
  selector:
    app: monitoramento
  ports:
  - port: 8080
    targetPort: http
    name: http
  type: ClusterIP
