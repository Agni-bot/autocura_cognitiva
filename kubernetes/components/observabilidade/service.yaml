apiVersion: v1
kind: Service
metadata:
  name: observabilidade
  namespace: autocura-cognitiva
  labels:
    app: observabilidade
    component: autocura-cognitiva
spec:
  selector:
    app: observabilidade
  ports:
  - port: 5000
    targetPort: http
    name: http
  type: ClusterIP
