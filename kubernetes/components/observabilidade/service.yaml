apiVersion: v1
kind: Service
metadata:
  name: observabilidade
  namespace: autocura-cognitiva
  labels:
    app: observabilidade
    component: autocura-cognitiva
spec:
  selector:
    app: observabilidade
  ports:
  - port: 8084
    targetPort: 8080
    name: http
  type: ClusterIP
