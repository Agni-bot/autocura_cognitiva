apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: autocura-cognitiva
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 10Gi 